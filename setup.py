#!/usr/bin/env python3
from setuptools import setup

setup(
    name='Matrix Encryption Simulator',
    version='1.0',
    description='Coming soon...',
    author='Jacob Dent',
    packages=['numpy', 'tkinter', 'binascii', 'os', 'sys'],  # same as name
    install_requires=['numpy', 'tkinter', 'binascii', 'os', 'sys'],  # external packages as dependencies
)
